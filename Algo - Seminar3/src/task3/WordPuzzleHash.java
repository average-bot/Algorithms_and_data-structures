package task3;
//Source: https://stackoverflow.com/questions/19513242/what-algorithm-to-use-for-a-word-search-using-a-hash-table

public class WordPuzzleHash {
    /*    set up hash table with all words
    char[][] grid = { { 't', 'h', 'i', 's' },
                          { 'w', 'a', 't', 's' },
                          { 'o', 'a', 'h', 'g' },
                          { 'f', 'g', 'd', 't' } };

    for each cell c
        findWords(c, c.value)

      findWords(cell c, string current)
        if current.length > longestWord
            return
        if hashTable.contains(current)
            System.out.println(current);
        for each neighbour n of c
          findWords(n, current + c.value)*/
}
